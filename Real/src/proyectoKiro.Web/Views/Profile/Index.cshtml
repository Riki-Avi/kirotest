@{
    ViewData["Title"] = "Perfil del Usuario - Kiro Code Lab";
}

<style>
    body {
        overflow-y: auto !important;
    }
    .profile-container {
        max-width: 960px;
        margin: 40px auto;
        padding: 0 20px;
    }
    .profile-card {
        background: var(--bg-card, #151C2C);
        border: 1px solid var(--border-color, rgba(255, 255, 255, 0.08));
        border-radius: var(--radius-lg, 18px);
        padding: 28px;
        display: flex;
        align-items: center;
        gap: 24px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
        margin-bottom: 30px;
        flex-wrap: wrap;
    }
    .avatar-wrapper {
        position: relative;
    }
    .profile-avatar {
        width: 90px;
        height: 90px;
        border-radius: 50%;
        object-fit: cover;
        border: 3px solid var(--accent-primary, #6366F1);
        box-shadow: 0 0 20px var(--accent-glow, rgba(99, 102, 241, 0.3));
    }
    .profile-info {
        flex: 1;
        min-width: 240px;
    }
    .profile-name {
        font-size: 1.8rem;
        font-weight: 700;
        color: var(--text-main, #F8FAFC);
        margin-bottom: 4px;
        display: flex;
        align-items: center;
        gap: 10px;
    }
    .profile-email {
        color: var(--text-muted, #94A3B8);
        font-size: 0.95rem;
        margin-bottom: 12px;
    }
    .badge-user {
        display: inline-block;
        background: rgba(99, 102, 241, 0.15);
        color: var(--accent-primary, #6366F1);
        border: 1px solid var(--border-highlight, rgba(99, 102, 241, 0.4));
        padding: 4px 12px;
        border-radius: 20px;
        font-size: 0.8rem;
        font-weight: 600;
    }
    .profile-actions {
        display: flex;
        gap: 12px;
        flex-wrap: wrap;
    }
    .stats-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 20px;
        margin-bottom: 35px;
    }
    .stat-card {
        background: var(--bg-card, #151C2C);
        border: 1px solid var(--border-color, rgba(255, 255, 255, 0.08));
        border-radius: var(--radius-md, 12px);
        padding: 20px;
        display: flex;
        align-items: center;
        gap: 16px;
    }
    .stat-icon {
        font-size: 2rem;
        background: rgba(255, 255, 255, 0.04);
        padding: 12px;
        border-radius: 12px;
    }
    .stat-value {
        font-size: 1.6rem;
        font-weight: 700;
        color: var(--text-main, #F8FAFC);
    }
    .stat-label {
        font-size: 0.85rem;
        color: var(--text-muted, #94A3B8);
    }
    .section-title {
        font-size: 1.3rem;
        font-weight: 600;
        margin-bottom: 18px;
        display: flex;
        align-items: center;
        gap: 10px;
    }
    .submissions-list {
        display: flex;
        flex-direction: column;
        gap: 16px;
    }
    .submission-card {
        background: var(--bg-card, #151C2C);
        border: 1px solid var(--border-color, rgba(255, 255, 255, 0.08));
        border-radius: var(--radius-md, 12px);
        padding: 20px;
        transition: border-color 0.2s ease;
    }
    .submission-card:hover {
        border-color: var(--border-highlight, rgba(99, 102, 241, 0.4));
    }
    .submission-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 12px;
        flex-wrap: wrap;
        gap: 10px;
    }
    .submission-title {
        font-weight: 600;
        font-size: 1.1rem;
        color: var(--text-main, #F8FAFC);
        display: flex;
        align-items: center;
        gap: 8px;
    }
    .submission-date {
        font-size: 0.85rem;
        color: var(--text-dim, #64748B);
    }
    .code-preview {
        background: #0B0F19;
        border: 1px solid rgba(255, 255, 255, 0.05);
        border-radius: 8px;
        padding: 14px;
        font-family: var(--font-mono, 'Fira Code', monospace);
        font-size: 0.88rem;
        color: #A5B4FC;
        overflow-x: auto;
        white-space: pre-wrap;
        max-height: 250px;
    }
    .badge-pass {
        background: rgba(16, 185, 129, 0.15);
        color: #10B981;
        border: 1px solid rgba(16, 185, 129, 0.3);
        padding: 4px 10px;
        border-radius: 6px;
        font-size: 0.8rem;
        font-weight: 600;
    }
    .empty-submissions {
        text-align: center;
        padding: 40px 20px;
        background: var(--bg-card, #151C2C);
        border: 1px solid var(--border-color, rgba(255, 255, 255, 0.08));
        border-radius: var(--radius-md, 12px);
        color: var(--text-muted, #94A3B8);
    }
</style>

<div class="profile-container">
    <!-- User Profile Header Card -->
    <div class="profile-card">
        <div class="avatar-wrapper">
            <img id="userAvatar" src="https://via.placeholder.com/90" alt="Avatar" class="profile-avatar" />
        </div>
        <div class="profile-info">
            <h1 class="profile-name" id="userName">
                Cargando usuario...
            </h1>
            <p class="profile-email" id="userEmail">Esperando sesión...</p>
            <span class="badge-user">⚡ Estudiante de C#</span>
        </div>
        <div class="profile-actions">
            <a href="/editor" class="btn-primary" style="padding: 10px 18px; border-radius: 8px; font-weight: 600; text-decoration: none; display: flex; align-items: center; gap: 8px;">
                💻 Ir al Editor
            </a>
            <button id="logoutBtn" class="btn-secondary" style="padding: 10px 18px; border-radius: 8px; font-weight: 600;">
                🚪 Cerrar Sesión
            </button>
        </div>
    </div>

    <!-- Stats Overview -->
    <div class="stats-grid">
        <div class="stat-card">
            <div class="stat-icon">🏆</div>
            <div>
                <div class="stat-value" id="completedCount">0</div>
                <div class="stat-label">Ejercicios Completados</div>
            </div>
        </div>
        <div class="stat-card">
            <div class="stat-icon">⚡</div>
            <div>
                <div class="stat-value">C# .NET</div>
                <div class="stat-label">Lenguaje Principal</div>
            </div>
        </div>
    </div>

    <!-- Section: Submitted / Completed Exercises -->
    <h2 class="section-title">
        📚 Historial de Ejercicios Completados
    </h2>
    
    <div class="submissions-list" id="submissionsContainer">
        <div class="empty-submissions">
            <p>⏳ Cargando ejercicios guardados en tu perfil...</p>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/@@supabase/supabase-js@2"></script>
<script>
    const SUPABASE_URL = "https://bzaifpevfuwlaznmhiee.supabase.co";
    const SUPABASE_ANON_KEY = "sb_publishable_xoU3gGiIKuchm6oTBhr8EQ_eb7sUD2a";
    const supabaseClient = supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);

    document.getElementById('logoutBtn').onclick = async () => {
        await supabaseClient.auth.signOut();
        window.location.href = '/Auth/Login';
    };

    supabaseClient.auth.onAuthStateChange(async (event, session) => {
        if (session?.user) {
            const metadata = session.user.user_metadata || {};
            const userId = session.user.id;

            document.getElementById('userName').textContent = metadata.full_name || session.user.email || 'Usuario Kiro';
            document.getElementById('userEmail').textContent = session.user.email;
            if (metadata.avatar_url) {
                document.getElementById('userAvatar').src = metadata.avatar_url;
            } else {
                document.getElementById('userAvatar').src = `https://ui-avatars.com/api/?name=${encodeURIComponent(metadata.full_name || 'U')}&background=6366F1&color=fff`;
            }

            // 1. Sincronizar usuario con PostgreSQL
            try {
                await fetch('/Auth/SyncUser', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        id: userId,
                        email: session.user.email,
                        nombreUsuario: metadata.full_name || session.user.email,
                        avatar: metadata.avatar_url || ''
                    })
                });
            } catch (err) {
                console.error("Error al sincronizar usuario:", err);
            }

            // 2. Cargar entregas del usuario desde /api/submissions/user/{userId}
            loadUserSubmissions(userId);

        } else {
            window.location.href = '/Auth/Login';
        }
    });

    async function loadUserSubmissions(userId) {
        const container = document.getElementById('submissionsContainer');
        try {
            const res = await fetch(`/api/submissions/user/${userId}`);
            if (!res.ok) throw new Error("Error al obtener entregas");

            const submissions = await res.json();
            document.getElementById('completedCount').textContent = submissions.length;

            if (submissions.length === 0) {
                container.innerHTML = `
                    <div class="empty-submissions">
                        <p>🧩 Aún no has completado ningún ejercicio.</p>
                        <p style="font-size:0.88rem; margin-top:8px;">Ve al <a href="/editor" style="color:var(--accent-primary);">Editor & Ejercicios</a>, ejecuta los tests y presiona "Completar Ejercicio".</p>
                    </div>
                `;
                return;
            }

            container.innerHTML = '';
            submissions.forEach(sub => {
                const dateStr = sub.submittedAt ? new Date(sub.submittedAt).toLocaleString('es-AR') : 'Fecha desconocida';
                const card = document.createElement('div');
                card.className = 'submission-card';
                card.innerHTML = `
                    <div class="submission-header">
                        <div class="submission-title">
                            <span>🧩 Ejercicio #${sub.exerciseId}</span>
                            <span class="badge-pass">✅ Completado</span>
                        </div>
                        <div class="submission-date">📅 ${dateStr}</div>
                    </div>
                    <p style="font-size:0.88rem; color:var(--text-muted); margin-bottom:8px;">Solución en C# guardada:</p>
                    <pre class="code-preview"><code>${escapeHtml(sub.submittedCode || '// Sin código')}</code></pre>
                `;
                container.appendChild(card);
            });

        } catch (err) {
            console.error("Error al cargar entregas:", err);
            container.innerHTML = `
                <div class="empty-submissions" style="color: var(--accent-danger);">
                    ❌ Ocurrió un error al cargar tus ejercicios completados.
                </div>
            `;
        }
    }

    function escapeHtml(str) {
        return (str || '')
            .replace(/&/g, "&amp;")
            .replace(/</g, "&lt;")
            .replace(/>/g, "&gt;")
            .replace(/"/g, "&quot;")
            .replace(/'/g, "&#039;");
    }
</script>